---
phase: 04-gdpr-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/package.json
  - site/src/lib/consent.ts
  - site/src/layouts/BaseLayout.astro
  - site/src/pages/privacy.astro
  - site/scripts/verify-phase4.mjs
autonomous: true
requirements:
  - PRIV-01
  - PRIV-02
  - PRIV-03
must_haves:
  truths:
    - "Visitor is not tracked by GA before explicit acceptance."
    - "Visitor can accept or reject from a bottom banner and dismissal behaves as reject."
    - "Visitor can reopen privacy settings from a footer link on any page and apply changes immediately."
    - "Visitor can open a privacy disclosure page that clearly explains provider, purpose, tracked data, and contact details."
  artifacts:
    - path: "site/src/lib/consent.ts"
      provides: "Consent state contract with cookie + localStorage persistence and accept/reject/unset helpers"
    - path: "site/src/layouts/BaseLayout.astro"
      provides: "Consent banner UI, reopen control, immediate update feedback, and consent-aware GA bootstrap gating"
    - path: "site/src/pages/privacy.astro"
      provides: "Dedicated plain-language tracking disclosure content"
    - path: "site/scripts/verify-phase4.mjs"
      provides: "Automated artifact checks for privacy route, disclosure markers, and consent-gated analytics behavior"
  key_links:
    - from: "site/src/layouts/BaseLayout.astro"
      to: "site/src/lib/consent.ts"
      via: "imported consent read/write helpers used by banner controls and gating"
      pattern: "from '../lib/consent'"
    - from: "site/src/layouts/BaseLayout.astro"
      to: "site/src/lib/analytics.ts"
      via: "production telemetry gate composed with consent acceptance state"
      pattern: "isProductionTelemetryEnabled"
    - from: "site/scripts/verify-phase4.mjs"
      to: "site/src/pages/privacy.astro"
      via: "dist HTML disclosure marker assertions"
      pattern: "privacy"
---

<objective>
Implement GDPR baseline behavior so analytics only runs after explicit consent, visitors can reopen and change consent at any time, and tracking disclosure is clearly available.

Purpose: Satisfy PRIV-01, PRIV-02, and PRIV-03 while preserving the existing Phase 03 production telemetry architecture.
Output: Consent state contract, consent-aware BaseLayout behavior, dedicated privacy page, and Phase 4 verifier automation.
</objective>

<execution_context>
@/home/mun/development/scipop/.opencode/get-shit-done/workflows/execute-plan.md
@/home/mun/development/scipop/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-gdpr-compliance/04-CONTEXT.md
@.planning/phases/04-gdpr-compliance/04-RESEARCH.md
@.planning/phases/03-analytics-baseline/03-analytics-baseline-01-SUMMARY.md

<interfaces>
From `site/src/lib/analytics.ts`:

```ts
export function getPublicMeasurementId(): string;
export function isProductionTelemetryEnabled(): boolean;
```

From `site/src/lib/paths.ts`:

```ts
export function withBase(path: string): string;
```

From `site/src/layouts/BaseLayout.astro` existing nav/footer shell:

```ts
interface Props {
  title: string;
}
```

New consent contract to implement in `site/src/lib/consent.ts`:

```ts
export type ConsentState = 'accepted' | 'rejected' | 'unset';
export const CONSENT_STORAGE_KEY: string;
export function readConsentState(): ConsentState;
export function writeConsentState(next: Exclude<ConsentState, 'unset'>): void;
export function clearConsentState(): void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add consent state library integration and persistence contract</name>
  <read_first>site/package.json, site/src/lib/analytics.ts, .planning/phases/04-gdpr-compliance/04-CONTEXT.md, .planning/phases/04-gdpr-compliance/04-RESEARCH.md</read_first>
  <files>site/package.json, site/src/lib/consent.ts</files>
  <behavior>
    - Test 1: `readConsentState()` returns `unset` when neither cookie nor localStorage key exists.
    - Test 2: `writeConsentState('accepted')` writes matching value to both cookie and localStorage.
    - Test 3: `writeConsentState('rejected')` persists rejection and this state is returned on subsequent reads.
  </behavior>
  <action>Add `vanilla-cookieconsent` dependency in `site/package.json` (per D-04 + specific idea to use a maintained library). Create `site/src/lib/consent.ts` with concrete exports: `CONSENT_STORAGE_KEY = 'scipop_analytics_consent'`; `readConsentState()` reads localStorage first then cookie fallback and normalizes to `'accepted' | 'rejected' | 'unset'`; `writeConsentState(next)` writes both `localStorage.setItem(CONSENT_STORAGE_KEY, next)` and `document.cookie = `${CONSENT_STORAGE_KEY}=${next}; Path=/; Max-Age=31536000; SameSite=Lax``; `clearConsentState()` removes both storages. Keep binary choices only (`accepted`, `rejected`) and no category/customize model (per D-05, D-06). Document inline that dismiss is handled as reject by caller (per D-03).</action>
  <acceptance_criteria>`site/package.json` contains `"vanilla-cookieconsent"`; `site/src/lib/consent.ts` exports `ConsentState`, `CONSENT_STORAGE_KEY`, `readConsentState`, `writeConsentState`, and `clearConsentState`; file contains storage key string `scipop_analytics_consent`; cookie write includes `SameSite=Lax` and one-year max-age.</acceptance_criteria>
  <verify>
    <automated>npm --prefix site install && npm --prefix site run build</automated>
  </verify>
  <done>Consent persistence contract exists, uses both cookie and localStorage (D-04), and supports only binary accept/reject states (D-05, D-06).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bottom consent banner, reopen settings, and consent-aware GA gating in BaseLayout</name>
  <read_first>site/src/layouts/BaseLayout.astro, site/src/lib/analytics.ts, site/src/lib/consent.ts, .planning/phases/04-gdpr-compliance/04-CONTEXT.md</read_first>
  <files>site/src/layouts/BaseLayout.astro</files>
  <behavior>
    - Test 1: GA loader/config is emitted only when production telemetry is enabled AND stored consent is `accepted`.
    - Test 2: First-visit users see bottom banner with only Accept all and Reject all controls.
    - Test 3: Footer privacy link reopens same controls and updates show inline success confirmation without reload.
  </behavior>
  <action>Update `BaseLayout.astro` to import `readConsentState` and `writeConsentState` from `../lib/consent`, then compute `const consentState = readConsentState();` and `const telemetryEnabled = isProductionTelemetryEnabled() && consentState === 'accepted';` so GA script never runs before consent (D-02). Keep existing GA privacy flags unchanged. Add bottom banner markup rendered when consent is unset or when user reopens settings: include short summary text plus links to `/privacy`; provide exactly two buttons labeled `Accept all` and `Reject all` (D-01, D-05, D-06, D-13). Add dismiss/close control that calls reject flow (`writeConsentState('rejected')`) (D-03). Add a footer on all pages with a link/button labeled `Privacy settings` that reopens the same banner/panel (no separate modal/page for controls) (D-09, D-10). In inline script, wire handlers so accept/reject writes consent immediately, updates banner visibility, and updates an inline status text `Privacy settings updated.` without page reload (D-11, D-12). Ensure switching accepted -> rejected immediately clears consent-enabled GA path by reloading analytics gate state variables for current session and preventing further GA config calls (D-07). Do not add consent-version re-prompt/version-bump behavior in this phase (D-08).</action>
  <acceptance_criteria>`BaseLayout.astro` contains `isProductionTelemetryEnabled() && consentState === 'accepted'`; banner block contains strings `Accept all` and `Reject all` and does not contain `Customize`; footer contains `Privacy settings`; dismiss handler writes rejected state; inline status text contains `Privacy settings updated.`; privacy link uses `/privacy` route path helper.</acceptance_criteria>
  <verify>
    <automated>npm --prefix site run build</automated>
  </verify>
  <done>Consent UI and analytics gate behavior satisfy D-01 through D-13 without introducing deferred customization/versioning features.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add privacy disclosure route and Phase 4 verification script</name>
  <read_first>site/src/pages/methodology.astro, site/scripts/verify-phase3.mjs, site/package.json, .planning/phases/04-gdpr-compliance/04-CONTEXT.md</read_first>
  <files>site/src/pages/privacy.astro, site/scripts/verify-phase4.mjs, site/package.json</files>
  <behavior>
    - Test 1: `/privacy` build artifact exists after `astro build`.
    - Test 2: disclosure page contains provider, purpose, tracked data/events, consent-change instructions, and contact details.
    - Test 3: phase verifier fails when disclosure markers or privacy settings affordance are missing.
  </behavior>
  <action>Create `site/src/pages/privacy.astro` using existing page/layout pattern with plain-language disclosure covering required sections (D-14, D-15): provider (`Google Analytics`), purpose (`aggregate visit measurement`), tracked data points/events (`page path`, `timestamp`, `device/browser basics`, `pageview event`), how to change consent (`Privacy settings` footer link), and contact details (`privacy@scipop.org` placeholder or project-approved address) (D-16). Create `site/scripts/verify-phase4.mjs` following phase verifier pattern to assert built `/privacy` route exists and required marker strings are present in generated HTML, plus assert BaseLayout artifact includes consent markers (`Accept all`, `Reject all`, `Privacy settings`) and does not include unconditional GA marker pattern without consent gate variable check. Add `verify:phase4` script in `site/package.json` mapped to `node ./scripts/verify-phase4.mjs`.</action>
  <acceptance_criteria>`site/src/pages/privacy.astro` exists and contains strings `Google Analytics`, `What we track`, `How to change consent`, and `Contact`; `site/scripts/verify-phase4.mjs` exists and exits non-zero on missing privacy/disclosure markers; `site/package.json` contains `verify:phase4` script.</acceptance_criteria>
  <verify>
    <automated>npm --prefix site run build && npm --prefix site run verify:phase4</automated>
  </verify>
  <done>Dedicated disclosure route and automated Phase 4 checks are in place so PRIV-03 and consent-gating regressions are enforceable.</done>
</task>

</tasks>

<verification>
Run `npm --prefix site run build && npm --prefix site run verify:phase1 && npm --prefix site run verify:phase2 && npm --prefix site run verify:phase3 && npm --prefix site run verify:phase4`.
</verification>

<success_criteria>
- GA tracking code is never emitted before explicit consent acceptance.
- Visitors can reopen consent controls from any page footer and changes apply immediately with confirmation text.
- `/privacy` is publicly available and includes plain-language disclosure of provider, purpose, tracked data/events, consent controls, and contact details.
- Automated phase verifier catches missing privacy route, missing disclosure copy markers, and missing consent UI markers.
</success_criteria>

<output>
After completion, create `.planning/phases/04-gdpr-compliance/04-gdpr-compliance-01-SUMMARY.md`
</output>
