---
phase: 04-gdpr-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/lib/consent.ts
  - site/src/lib/analytics.ts
  - site/src/layouts/BaseLayout.astro
autonomous: true
requirements:
  - TBD
must_haves:
  truths:
    - "Visitor is not tracked by GA before making a consent choice."
    - "Visitor can explicitly accept or decline analytics cookies from a visible consent prompt."
    - "Analytics bootstrap only runs after explicit consent and still keeps existing privacy-safe GA flags."
  artifacts:
    - path: "site/src/lib/consent.ts"
      provides: "Consent-state contract and storage-key constants"
    - path: "site/src/lib/analytics.ts"
      provides: "Telemetry helper surface that supports consent-gated initialization"
    - path: "site/src/layouts/BaseLayout.astro"
      provides: "Consent banner + runtime consent handling + deferred GA bootstrap"
  key_links:
    - from: "site/src/layouts/BaseLayout.astro"
      to: "site/src/lib/consent.ts"
      via: "shared storage key and consent-state literals"
      pattern: "SCIPOP_ANALYTICS_CONSENT_KEY"
    - from: "site/src/layouts/BaseLayout.astro"
      to: "https://www.googletagmanager.com/gtag/js"
      via: "dynamic loader only after consent=granted"
      pattern: "if \(consentState === 'granted'\)"
---

<objective>
Implement the GDPR consent gate that blocks non-essential analytics by default and initializes GA only after explicit visitor consent.

Purpose: Establish the legal/technical foundation for Phase 4 by changing analytics from implicit loading to explicit opt-in.
Output: Consent contract module, consent-aware analytics helpers, and BaseLayout consent/runtime bootstrap wiring.
</objective>

<execution_context>
@/home/mun/development/scipop/.opencode/get-shit-done/workflows/execute-plan.md
@/home/mun/development/scipop/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@site/src/lib/analytics.ts
@site/src/layouts/BaseLayout.astro

<interfaces>
From `site/src/lib/analytics.ts`:

```ts
export const SITE_ENV_PRODUCTION = 'production';
export function getSiteEnv(): string;
export function getPublicMeasurementId(): string;
export function isProductionTelemetryEnabled(): boolean;
export function assertProductionTelemetryConfig(): void;
```

New consent contract to create:

```ts
export const SCIPOP_ANALYTICS_CONSENT_KEY = 'scipop_analytics_consent';
export type AnalyticsConsentState = 'granted' | 'denied' | 'unset';
export function parseAnalyticsConsent(value: string | null): AnalyticsConsentState;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GDPR consent-state contract and consent-aware analytics helpers</name>
  <read_first>site/src/lib/analytics.ts</read_first>
  <files>site/src/lib/consent.ts, site/src/lib/analytics.ts</files>
  <action>Create `site/src/lib/consent.ts` with concrete exports: `SCIPOP_ANALYTICS_CONSENT_KEY = 'scipop_analytics_consent'`, `AnalyticsConsentState` union (`'granted' | 'denied' | 'unset'`), and `parseAnalyticsConsent(value)` that returns `granted` only for exact string `granted`, `denied` only for exact string `denied`, otherwise `unset`. In `site/src/lib/analytics.ts`, keep existing production guard functions and add explicit runtime helpers for deferred bootstrap: `buildGaScriptUrl(measurementId: string): string` returning `https://www.googletagmanager.com/gtag/js?id=${measurementId}`, and `buildGaConfigPayload()` returning `{ anonymize_ip: true, allow_google_signals: false, allow_ad_personalization_signals: false, page_path: window.location.pathname }`. Do not add any implicit-consent fallback; unset consent must remain non-tracking.</action>
  <acceptance_criteria>`site/src/lib/consent.ts` exports the exact key `scipop_analytics_consent`, the 3-state union, and parser behavior for unknown values; `site/src/lib/analytics.ts` exports deterministic GA URL/config helpers with the three privacy flags and `window.location.pathname` page path.</acceptance_criteria>
  <verify>
    <automated>rg "SCIPOP_ANALYTICS_CONSENT_KEY|AnalyticsConsentState|parseAnalyticsConsent|buildGaScriptUrl|buildGaConfigPayload|anonymize_ip: true|allow_google_signals: false|allow_ad_personalization_signals: false" site/src/lib/consent.ts site/src/lib/analytics.ts</automated>
  </verify>
  <done>Consent and analytics helper contracts exist and are reusable by layout/runtime code without ad hoc string literals.</done>
</task>

<task type="auto">
  <name>Task 2: Gate GA bootstrap behind explicit consent in BaseLayout</name>
  <read_first>site/src/layouts/BaseLayout.astro, site/src/lib/consent.ts, site/src/lib/analytics.ts</read_first>
  <files>site/src/layouts/BaseLayout.astro</files>
  <action>Refactor `site/src/layouts/BaseLayout.astro` to remove unconditional GA script tags from `<head>`. Add a consent banner container with `data-consent-banner`, two buttons `data-consent-accept` and `data-consent-decline`, and copy explaining analytics are optional. In the inline script: read `localStorage.getItem('scipop_analytics_consent')`, normalize via consent parser logic, and default to `unset`. When state is `unset`, show banner and do not load GA. When `granted`, dynamically append the GA loader script URL `https://www.googletagmanager.com/gtag/js?id=${measurementId}` and run `gtag('config', measurementId, { anonymize_ip: true, allow_google_signals: false, allow_ad_personalization_signals: false, page_path: window.location.pathname })`. When `denied`, keep GA unloaded. Persist button choices as literal `granted`/`denied` in localStorage under `scipop_analytics_consent`. Keep existing production env guard (`assertProductionTelemetryConfig`) so missing measurement ID still fails production builds.</action>
  <acceptance_criteria>BaseLayout contains consent banner controls (`data-consent-banner`, `data-consent-accept`, `data-consent-decline`); no static `<script async src="https://www.googletagmanager.com/gtag/js...">` is emitted before consent; GA script injection path exists only in the `granted` consent branch and retains the three privacy flags.</acceptance_criteria>
  <verify>
    <automated>PUBLIC_SITE_ENV=production PUBLIC_GA_MEASUREMENT_ID=G-TEST123 npm --prefix site run build && node site/scripts/verify-phase3.mjs</automated>
  </verify>
  <done>Built pages no longer rely on implicit analytics loading; GA initialization requires explicit visitor opt-in while preserving prior telemetry safeguards.</done>
</task>

</tasks>

<verification>
Run `PUBLIC_SITE_ENV=production PUBLIC_GA_MEASUREMENT_ID=G-TEST123 npm --prefix site run build` and confirm consent UI appears before analytics initialization in built HTML/runtime.
</verification>

<success_criteria>
- Non-essential analytics is blocked by default for first-time visitors.
- Explicit accept/decline choices are captured in localStorage and deterministically drive GA loading behavior.
- Existing production telemetry safety checks remain intact.
</success_criteria>

<output>
After completion, create `.planning/phases/04-gdpr-compliance/04-gdpr-compliance-01-SUMMARY.md`
</output>
