---
phase: 04-gdpr-compliance
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - site/scripts/verify-phase4.mjs
  - site/package.json
  - .planning/phases/04-gdpr-compliance/04-GDPR-VERIFICATION-CHECKLIST.md
autonomous: true
requirements:
  - TBD
must_haves:
  truths:
    - "Automated verification fails if consent controls/privacy route are missing from built public artifacts."
    - "Automated verification fails if GA loader is statically injected before consent-gated runtime logic."
    - "Project owner has a repeatable manual checklist for consent grant/deny/withdraw behavior validation."
  artifacts:
    - path: "site/scripts/verify-phase4.mjs"
      provides: "Phase 4 static artifact verifier for GDPR consent baseline"
    - path: "site/package.json"
      provides: "`verify:phase4` executable command"
    - path: ".planning/phases/04-gdpr-compliance/04-GDPR-VERIFICATION-CHECKLIST.md"
      provides: "Manual compliance checklist and evidence capture template"
  key_links:
    - from: "site/package.json"
      to: "site/scripts/verify-phase4.mjs"
      via: "npm script mapping"
      pattern: "verify:phase4"
    - from: "site/scripts/verify-phase4.mjs"
      to: "site/src/layouts/BaseLayout.astro"
      via: "required consent/privacy markers in built HTML"
      pattern: "data-consent-banner"
    - from: ".planning/phases/04-gdpr-compliance/04-GDPR-VERIFICATION-CHECKLIST.md"
      to: "browser runtime behavior"
      via: "grant, deny, and withdraw evidence steps"
      pattern: "Status: pending | passed | failed"
---

<objective>
Add deterministic verification for GDPR compliance behaviors so consent protections and public disclosures are enforced before release.

Purpose: Prevent regression of consent gating/privacy visibility by pairing build checks with a manual runtime evidence protocol.
Output: `verify:phase4` script, package command wiring, and GDPR verification checklist artifact.
</objective>

<execution_context>
@/home/mun/development/scipop/.opencode/get-shit-done/workflows/execute-plan.md
@/home/mun/development/scipop/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@site/scripts/verify-phase3.mjs
@site/package.json
@site/src/layouts/BaseLayout.astro

<interfaces>
From `site/scripts/verify-phase3.mjs` reusable pattern:

```ts
const coreRoutes = ['/', '/featured', '/methodology'];
const requiredRoutes = [...coreRoutes, ...approvedSynthesisRoutes];
```

New phase-4 checks to enforce:

```ts
// Required route added in phase 4:
'/privacy'

// Required consent markers in built HTML:
'data-consent-banner'
'data-consent-accept'
'data-consent-decline'
'data-consent-manage'
"scipop_analytics_consent"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Phase 4 GDPR static artifact verifier and package script wiring</name>
  <read_first>site/scripts/verify-phase3.mjs, site/package.json</read_first>
  <files>site/scripts/verify-phase4.mjs, site/package.json</files>
  <action>Create `site/scripts/verify-phase4.mjs` by extending phase verifier conventions: discover approved synthesis routes, enforce core routes `/`, `/featured`, `/methodology`, and add required route `/privacy`. For each required route HTML artifact, assert marker presence: `data-consent-banner`, `data-consent-accept`, `data-consent-decline`, `data-consent-manage`, and `scipop_analytics_consent`. Add regression check that fails when static HTML contains direct GA loader tag string `googletagmanager.com/gtag/js?id=` before consent handling (this enforces deferred runtime loading). Wire command in `site/package.json` exactly as `"verify:phase4": "node ./scripts/verify-phase4.mjs"` while keeping phase 1-3 commands unchanged.</action>
  <acceptance_criteria>`site/scripts/verify-phase4.mjs` validates `/privacy` plus existing public routes; script checks all five consent markers and fails on static GA loader marker; `site/package.json` contains `verify:phase4` mapped to `node ./scripts/verify-phase4.mjs`.</acceptance_criteria>
  <verify>
    <automated>PUBLIC_SITE_ENV=production PUBLIC_GA_MEASUREMENT_ID=G-TEST123 npm --prefix site run build && npm --prefix site run verify:phase4</automated>
  </verify>
  <done>Phase 4 has an executable build-artifact guardrail that prevents silent regressions in consent and disclosure wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Create GDPR manual verification checklist with evidence protocol</name>
  <read_first>.planning/phases/03-analytics-baseline/03-GA-VERIFICATION-CHECKLIST.md, site/scripts/verify-phase4.mjs</read_first>
  <files>.planning/phases/04-gdpr-compliance/04-GDPR-VERIFICATION-CHECKLIST.md</files>
  <action>Create `.planning/phases/04-gdpr-compliance/04-GDPR-VERIFICATION-CHECKLIST.md` with exact sections: `## Automated preflight`, `## First-visit consent behavior`, `## Grant consent behavior`, `## Deny/withdraw consent behavior`, and `## Evidence log`. Include explicit commands in preflight: `npm --prefix site run build` and `npm --prefix site run verify:phase4`. Include manual steps requiring browser storage inspection for key `scipop_analytics_consent`, Network inspection for GA requests only after grant, and confirmation that deny/withdraw path prevents further GA requests and re-shows preferences UI. Add evidence table columns exactly: `scenario`, `route`, `observed_result`, `timestamp_utc`, `notes`. End file with `Status: pending | passed | failed`.</action>
  <acceptance_criteria>Checklist file contains all five required headings, both automated commands, storage key checks, network checks for grant/deny cases, required evidence table columns, and final status line.</acceptance_criteria>
  <verify>
    <automated>rg "Automated preflight|First-visit consent behavior|Grant consent behavior|Deny/withdraw consent behavior|Evidence log|scipop_analytics_consent|npm --prefix site run verify:phase4|Status: pending \| passed \| failed" .planning/phases/04-gdpr-compliance/04-GDPR-VERIFICATION-CHECKLIST.md</automated>
  </verify>
  <done>Manual runtime compliance validation is documented with clear evidence capture for auditability.</done>
</task>

</tasks>

<verification>
Run `PUBLIC_SITE_ENV=production PUBLIC_GA_MEASUREMENT_ID=G-TEST123 npm --prefix site run build && npm --prefix site run verify:phase4`.
</verification>

<success_criteria>
- `verify:phase4` enforces presence of consent/privacy markers and absence of implicit static GA loading.
- Manual checklist covers first-visit, grant, deny, and withdrawal scenarios with timestamped evidence.
- GDPR phase outputs are verifiable via both CLI automation and human runtime checks.
</success_criteria>

<output>
After completion, create `.planning/phases/04-gdpr-compliance/04-gdpr-compliance-03-SUMMARY.md`
</output>
