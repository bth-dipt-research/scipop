---
phase: 04-gdpr-compliance
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - content/pages/privacy.md
  - site/src/pages/privacy.astro
  - site/src/layouts/BaseLayout.astro
autonomous: true
requirements:
  - TBD
must_haves:
  truths:
    - "Visitor can open a public privacy page that explains analytics consent behavior and withdrawal steps."
    - "Visitor can reopen consent controls after initial choice and change consent state."
    - "Withdrawing consent stops future analytics loading and clears first-party GA cookies where possible."
  artifacts:
    - path: "content/pages/privacy.md"
      provides: "Public-facing privacy and cookie disclosure copy"
    - path: "site/src/pages/privacy.astro"
      provides: "Stable static privacy route rendering markdown content"
    - path: "site/src/layouts/BaseLayout.astro"
      provides: "Persistent privacy link and consent-preferences trigger"
  key_links:
    - from: "site/src/layouts/BaseLayout.astro"
      to: "site/src/pages/privacy.astro"
      via: "primary navigation and footer privacy links"
      pattern: "href={withBase('/privacy')}"
    - from: "site/src/layouts/BaseLayout.astro"
      to: "localStorage scipop_analytics_consent"
      via: "privacy-settings control reopening consent banner"
      pattern: "data-consent-manage"
---

<objective>
Add the user-facing GDPR disclosure and consent-management surface so visitors can review privacy terms and change analytics consent at any time.

Purpose: Complete the consent lifecycle beyond first prompt by adding transparency and withdrawal controls.
Output: `/privacy` route content + layout navigation + consent preference reopening/withdraw handling.
</objective>

<execution_context>
@/home/mun/development/scipop/.opencode/get-shit-done/workflows/execute-plan.md
@/home/mun/development/scipop/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@site/src/layouts/BaseLayout.astro
@site/src/pages/methodology.astro
@content/pages/methodology.md

<interfaces>
From `site/src/pages/methodology.astro` pattern:

```ts
const pages = await getCollection('pages');
const entry = pages.find((item) => item.data.slug === '...');
const { Content } = await render(entry);
```

Existing nav base-path helper:

```ts
import { withBase } from '../lib/paths';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish privacy disclosure content and static /privacy route</name>
  <read_first>content/pages/methodology.md, site/src/pages/methodology.astro</read_first>
  <files>content/pages/privacy.md, site/src/pages/privacy.astro</files>
  <action>Create `content/pages/privacy.md` frontmatter with `title: "Privacy Notice"` and `slug: "privacy"`. In markdown body, include explicit sections with exact headings: `## What we collect`, `## Analytics consent`, `## How to withdraw consent`, and `## Contact`. State that analytics is optional, disabled by default until consent is granted, and can be changed anytime via "Privacy settings". Create `site/src/pages/privacy.astro` following methodology route pattern: load `pages` collection, find `slug === 'privacy'`, throw explicit error if missing, render markdown content in `BaseLayout`, and set page title from frontmatter.</action>
  <acceptance_criteria>`content/pages/privacy.md` exists with the four required headings; `/privacy` route source (`site/src/pages/privacy.astro`) resolves page entry by slug `privacy` and throws a clear error when missing; route uses BaseLayout for consistent public navigation.</acceptance_criteria>
  <verify>
    <automated>npm --prefix site run build && rg "## What we collect|## Analytics consent|## How to withdraw consent|## Contact" content/pages/privacy.md</automated>
  </verify>
  <done>Privacy notice is published as markdown-backed static content and available through a stable site route.</done>
</task>

<task type="auto">
  <name>Task 2: Add persistent privacy navigation and consent preference management controls</name>
  <read_first>site/src/layouts/BaseLayout.astro, site/src/lib/paths.ts</read_first>
  <files>site/src/layouts/BaseLayout.astro</files>
  <action>Update `BaseLayout` navigation to include a new link `href={withBase('/privacy')}` labeled `Privacy`. Add a persistent button labeled `Privacy settings` with selector `data-consent-manage` (header or footer) that reopens consent UI after initial choice. Implement runtime behavior so clicking this control reveals the consent banner and allows rewriting `scipop_analytics_consent` to `granted` or `denied`. On change to `denied`, remove first-party GA cookies by expiring `_ga`, `_gid`, and `_ga_*` cookies on path `/`, and prevent GA bootstrap on subsequent page loads unless consent returns to `granted`. Keep behavior deterministic in production and non-production modes.</action>
  <acceptance_criteria>BaseLayout contains `href={withBase('/privacy')}` and `data-consent-manage`; consent banner can be reopened after initial selection; denial path includes cookie-expiry logic for `_ga`, `_gid`, and `_ga_*` markers and blocks future GA loading by persisted `denied` state.</acceptance_criteria>
  <verify>
    <automated>PUBLIC_SITE_ENV=production PUBLIC_GA_MEASUREMENT_ID=G-TEST123 npm --prefix site run build && rg "withBase\('/privacy'\)|data-consent-manage|_ga|_gid|scipop_analytics_consent" site/src/layouts/BaseLayout.astro</automated>
  </verify>
  <done>Visitors can always access privacy information and modify analytics consent after initial interaction.</done>
</task>

</tasks>

<verification>
Run `npm --prefix site run build` then confirm `/privacy` is generated and layout includes the privacy link + manage-consent control markers.
</verification>

<success_criteria>
- Site exposes a dedicated privacy notice route with clear consent and withdrawal language.
- Consent choices can be revisited through a persistent UI control.
- Denied state remains durable and suppresses future analytics initialization.
</success_criteria>

<output>
After completion, create `.planning/phases/04-gdpr-compliance/04-gdpr-compliance-02-SUMMARY.md`
</output>
