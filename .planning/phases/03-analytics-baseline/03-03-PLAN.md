---
phase: 03-analytics-baseline
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - site/scripts/verify-phase3-runtime-smoke.mjs
  - site/package.json
  - .planning/phases/03-analytics-baseline/03-GA-VERIFICATION-CHECKLIST.md
autonomous: true
requirements:
  - ANLT-01
gap_closure: true
must_haves:
  truths:
    - "Project owner can run a production-mode runtime smoke check that confirms outbound GA-domain requests are possible before manual UAT review."
    - "Manual UAT instructions explicitly prevent false negatives caused by tracker blocking or DevTools filtering."
    - "Evidence capture for GA runtime request visibility includes request-domain proof, not only static HTML marker checks."
  artifacts:
    - path: "site/scripts/verify-phase3-runtime-smoke.mjs"
      provides: "Optional runtime smoke validation for outbound GA-domain requests under production env settings"
    - path: "site/package.json"
      provides: "Named npm command for runtime smoke execution"
    - path: ".planning/phases/03-analytics-baseline/03-GA-VERIFICATION-CHECKLIST.md"
      provides: "Explicit anti-false-negative protocol and runtime request evidence table"
  key_links:
    - from: "site/package.json"
      to: "site/scripts/verify-phase3-runtime-smoke.mjs"
      via: "npm script mapping"
      pattern: "verify:phase3:runtime-smoke"
    - from: ".planning/phases/03-analytics-baseline/03-GA-VERIFICATION-CHECKLIST.md"
      to: "Browser DevTools Network panel"
      via: "Required filter/blocker reset steps before request validation"
      pattern: "Disable tracker/ad-blocking"
    - from: ".planning/phases/03-analytics-baseline/03-GA-VERIFICATION-CHECKLIST.md"
      to: "GA runtime evidence"
      via: "Request-domain capture rows for googletagmanager.com and google-analytics.com"
      pattern: "request_domain"
---

<objective>
Close the diagnosed Phase 3 UAT reliability gap by adding runtime-oriented GA request validation support and hardening manual observation protocol.

Purpose: Address the UAT failure root cause (environment-dependent request visibility) while preserving locked analytics decisions and keeping scope strictly within ANLT-01.
Output: Runtime smoke helper command + checklist updates that make request-observation failures actionable and reproducible.
</objective>

<execution_context>
@/home/mun/development/scipop/.opencode/get-shit-done/workflows/execute-plan.md
@/home/mun/development/scipop/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-analytics-baseline/03-UAT.md
@.planning/phases/03-analytics-baseline/03-CONTEXT.md
@.planning/phases/03-analytics-baseline/03-GA-VERIFICATION-CHECKLIST.md
@site/scripts/verify-phase3.mjs
@site/src/lib/analytics.ts

<interfaces>
From `site/src/lib/analytics.ts`:

```ts
export const SITE_ENV_PRODUCTION = 'production';
export function getSiteEnv(): string;
export function getPublicMeasurementId(): string;
export function isProductionTelemetryEnabled(): boolean;
```

From `site/package.json` scripts:

```json
{
  "verify:phase3": "node ./scripts/verify-phase3.mjs"
}
```

New runtime smoke contract to create:

```ts
// site/scripts/verify-phase3-runtime-smoke.mjs
// Required behavior:
// - Fail fast when PUBLIC_SITE_ENV is not production or measurement ID is missing.
// - Attempt outbound requests to GA domains used by runtime telemetry flow.
// - Emit structured pass/fail summary with domain-level results.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add optional runtime GA request smoke command for production-mode environments</name>
  <files>site/scripts/verify-phase3-runtime-smoke.mjs, site/package.json</files>
  <action>Create `site/scripts/verify-phase3-runtime-smoke.mjs` to close the missing runtime-validation coverage from UAT gap test 2. The script must: (1) read `PUBLIC_SITE_ENV` and `PUBLIC_GA_MEASUREMENT_ID`; (2) exit non-zero with clear remediation text unless `PUBLIC_SITE_ENV === 'production'` and measurement ID is non-empty (per D-09, D-11); (3) perform outbound HTTP checks to both GA runtime domains used in observation (`https://www.googletagmanager.com/gtag/js?id={measurementId}` and `https://www.google-analytics.com/g/collect?...` using non-PII synthetic parameters) and record status codes/timeouts; (4) fail non-zero if either domain check fails; (5) print a compact pass summary including checked domains and timestamps so output can be pasted into UAT notes. Wire command in `site/package.json` as `"verify:phase3:runtime-smoke": "node ./scripts/verify-phase3-runtime-smoke.mjs"`. Keep this command additive (do not replace existing `verify:phase3`) and document in script header that this is an optional runtime smoke companion to static artifact verification, addressing UAT-diagnosed runtime visibility limitations.</action>
  <verify>
    <automated>PUBLIC_SITE_ENV=production PUBLIC_GA_MEASUREMENT_ID=G-TEST123 npm --prefix site run verify:phase3:runtime-smoke</automated>
  </verify>
  <done>Repository has a runnable production-mode runtime smoke command that validates outbound connectivity to both GA request domains and returns actionable failure messages.</done>
</task>

<task type="auto">
  <name>Task 2: Harden manual GA checklist with anti-false-negative observation protocol and runtime evidence fields</name>
  <files>.planning/phases/03-analytics-baseline/03-GA-VERIFICATION-CHECKLIST.md</files>
  <action>Update the checklist to explicitly implement UAT gap missing items: add a preflight subsection requiring browser tracker/ad-block extensions disabled, strict privacy protections relaxed for test session, DevTools Network filter cleared (no `localhost`-only filter), and `Preserve log` enabled before route visits. Add explicit request-domain validation steps requiring visible requests to both `googletagmanager.com` and `google-analytics.com` while visiting required routes (per D-01, D-13, D-15). Add a new optional section titled `## Optional runtime smoke check (production env)` that runs `npm --prefix site run verify:phase3:runtime-smoke` with required env vars and captures command output in evidence notes. Expand evidence tables to include `request_domain`, `request_seen`, and `notes_if_missing` columns so recurrence is prevented by making blockers/filters diagnosable from captured evidence.</action>
  <verify>
    <automated>rg "Disable tracker|Network filter|googletagmanager.com|google-analytics.com|Optional runtime smoke check \(production env\)|request_domain|notes_if_missing" .planning/phases/03-analytics-baseline/03-GA-VERIFICATION-CHECKLIST.md</automated>
  </verify>
  <done>Manual UAT instructions now explicitly guard against hidden-blocked GA requests and require domain-level runtime evidence capture for each verification run.</done>
</task>

</tasks>

<verification>
Run `npm --prefix site run build && npm --prefix site run verify:phase3 && PUBLIC_SITE_ENV=production PUBLIC_GA_MEASUREMENT_ID=G-TEST123 npm --prefix site run verify:phase3:runtime-smoke`.
</verification>

<success_criteria>
- UAT operators have deterministic preflight steps that remove common GA request visibility false negatives.
- A production-mode runtime smoke command exists and reports pass/fail against GA outbound domains.
- Manual evidence capture now includes explicit request-domain proof and troubleshooting notes for missing requests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-analytics-baseline/03-analytics-baseline-03-SUMMARY.md`
</output>
