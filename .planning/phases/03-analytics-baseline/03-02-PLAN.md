---
phase: 03-analytics-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - site/scripts/verify-phase3.mjs
  - site/package.json
  - .planning/phases/03-analytics-baseline/03-GA-VERIFICATION-CHECKLIST.md
autonomous: true
requirements:
  - ANLT-01
must_haves:
  truths:
    - "Automated verification fails if GA telemetry markers are missing from any required public-route build artifact."
    - "Project owner has an explicit checklist to confirm GA real-time and standard-report visibility after deployment."
    - "Phase verification includes both automated static checks and manual evidence capture steps."
  artifacts:
    - path: "site/scripts/verify-phase3.mjs"
      provides: "Route-level GA snippet/config coverage validation for Phase 3"
    - path: "site/package.json"
      provides: "`verify:phase3` command wired into site workflow"
    - path: ".planning/phases/03-analytics-baseline/03-GA-VERIFICATION-CHECKLIST.md"
      provides: "Timestamped real-time + 24-hour report evidence template"
  key_links:
    - from: "site/scripts/verify-phase3.mjs"
      to: "site/src/layouts/BaseLayout.astro"
      via: "HTML marker assertions for GA loader/config/privacy fields"
      pattern: "anonymize_ip: true"
    - from: "site/package.json"
      to: "site/scripts/verify-phase3.mjs"
      via: "npm script mapping"
      pattern: "verify:phase3"
    - from: ".planning/phases/03-analytics-baseline/03-GA-VERIFICATION-CHECKLIST.md"
      to: "GA console validation workflow"
      via: "manual checklist and evidence table"
      pattern: "Realtime verification checklist"
---

<objective>
Add deterministic Phase 3 verification and manual evidence workflow so analytics baseline quality can be proven before and after deployment.

Purpose: Close ANLT-01 verification obligations from locked decisions D-13..D-16 with both script enforcement and operational checklisting.
Output: `verify:phase3` script + package wiring + GA manual verification checklist artifact.
</objective>

<execution_context>
@/home/mun/development/scipop/.opencode/get-shit-done/workflows/execute-plan.md
@/home/mun/development/scipop/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-analytics-baseline/03-CONTEXT.md
@.planning/phases/03-analytics-baseline/03-RESEARCH.md
@site/scripts/verify-phase1.mjs
@site/scripts/verify-phase2.mjs

<interfaces>
From existing verifier pattern (`verify-phase1.mjs`, `verify-phase2.mjs`):

```ts
const siteDir = process.cwd();
const distDir = path.join(siteDir, 'dist');
const basePrefix = 'scipop';
```

Route requirements for Phase 3 (per D-01):

```ts
const coreRoutes = ['/', '/featured', '/methodology'];
// plus each approved /syntheses/{slug} discovered from content files
```

Expected Phase 3 marker literals in built HTML:

```ts
[
  'googletagmanager.com/gtag/js?id=',
  "gtag('config'",
  'anonymize_ip: true',
  'allow_google_signals: false',
  'allow_ad_personalization_signals: false',
  'window.location.pathname'
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Phase 3 GA build-artifact verifier</name>
  <read_first>site/scripts/verify-phase1.mjs, site/scripts/verify-phase2.mjs, site/src/layouts/BaseLayout.astro</read_first>
  <files>site/scripts/verify-phase3.mjs</files>
  <action>Create `site/scripts/verify-phase3.mjs` following existing verifier structure. The script must: (1) discover approved synthesis slugs from `../content/syntheses/*.md` by `status: approved` and `slug:` parsing; (2) build required route list containing `/`, `/featured`, `/methodology`, and `/syntheses/{slug}` (D-01, D-14); (3) resolve each route to dist candidates with and without `/scipop` base prefix; (4) fail when any required route artifact is missing; (5) read each required HTML artifact and fail when any telemetry marker is absent: `googletagmanager.com/gtag/js?id=`, `gtag('config'`, `anonymize_ip: true`, `allow_google_signals: false`, `allow_ad_personalization_signals: false`, `window.location.pathname` (D-02, D-07, D-08, D-14); (6) print a success line including verified route count.</action>
  <acceptance_criteria>`site/scripts/verify-phase3.mjs` exists; includes required route set with `/`, `/featured`, `/methodology` and approved syntheses; includes all six telemetry marker checks; exits non-zero on missing routes or markers and logs human-readable error categories.</acceptance_criteria>
  <verify>
    <automated>npm --prefix site run build && node site/scripts/verify-phase3.mjs</automated>
  </verify>
  <done>Phase 3 has deterministic artifact-level enforcement for analytics route coverage and required GA config markers.</done>
</task>

<task type="auto">
  <name>Task 2: Wire verify:phase3 into package scripts and full verification flow</name>
  <read_first>site/package.json, site/scripts/verify-phase3.mjs</read_first>
  <files>site/package.json</files>
  <action>Add npm script entry `"verify:phase3": "node ./scripts/verify-phase3.mjs"` to `site/package.json`. Keep existing `verify:phase1` and `verify:phase2` unchanged. Ensure the command is callable via `npm --prefix site run verify:phase3` and include it in plan-level verification command chain (D-13, D-14).</action>
  <acceptance_criteria>`site/package.json` contains `verify:phase3` script mapped to `node ./scripts/verify-phase3.mjs`; `npm --prefix site run verify:phase3` succeeds after build.</acceptance_criteria>
  <verify>
    <automated>npm --prefix site run build && npm --prefix site run verify:phase1 && npm --prefix site run verify:phase2 && npm --prefix site run verify:phase3</automated>
  </verify>
  <done>Phase verification workflow now includes analytics checks in the same command-driven pattern as previous phases.</done>
</task>

<task type="auto">
  <name>Task 3: Create manual GA evidence checklist for realtime and 24-hour reports</name>
  <read_first>.planning/phases/03-analytics-baseline/03-CONTEXT.md, .planning/phases/02-readability-accessibility-baseline/02-VERIFICATION.md</read_first>
  <files>.planning/phases/03-analytics-baseline/03-GA-VERIFICATION-CHECKLIST.md</files>
  <action>Create `.planning/phases/03-analytics-baseline/03-GA-VERIFICATION-CHECKLIST.md` with two explicit sections: `## Realtime verification checklist` and `## 24-hour standard report checklist`. Realtime section must include step-by-step instructions to visit `/`, `/featured`, `/methodology`, and at least one `/syntheses/{slug}` route on production, then confirm those route paths in GA Realtime with timestamp and evidence notes table columns (`route`, `visit_time_utc`, `ga_view`, `evidence_note`) (D-13, D-15). 24-hour section must require checking GA standard report within 24h and recording table columns (`report_name`, `checked_time_utc`, `routes_confirmed`, `evidence_note`) (D-16). Add a final sign-off line: `Status: pending | passed | failed`.</action>
  <acceptance_criteria>Checklist file exists with exact headings `Realtime verification checklist` and `24-hour standard report checklist`; includes required route list and evidence table columns; includes `Status: pending | passed | failed` line.</acceptance_criteria>
  <verify>
    <automated>rg "Realtime verification checklist|24-hour standard report checklist|Status: pending \| passed \| failed" .planning/phases/03-analytics-baseline/03-GA-VERIFICATION-CHECKLIST.md</automated>
  </verify>
  <done>Manual GA console validation can be executed consistently with timestamped evidence for both immediate and delayed reporting windows.</done>
</task>

</tasks>

<verification>
Run `npm --prefix site run build && npm --prefix site run verify:phase1 && npm --prefix site run verify:phase2 && npm --prefix site run verify:phase3`.
</verification>

<success_criteria>
- `verify:phase3` fails on missing GA markers for any required public route artifact.
- Phase runbook includes timestamped realtime evidence and mandatory 24-hour standard report confirmation steps.
- ANLT-01 has both automated and manual verification paths documented and executable.
</success_criteria>

<output>
After completion, create `.planning/phases/03-analytics-baseline/03-analytics-baseline-02-SUMMARY.md`
</output>
