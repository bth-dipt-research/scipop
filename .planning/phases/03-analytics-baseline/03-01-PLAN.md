---
phase: 03-analytics-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/lib/analytics.ts
  - site/src/layouts/BaseLayout.astro
autonomous: true
requirements:
  - ANLT-01
user_setup:
  - service: google-analytics
    why: "GA4 pageview telemetry for public MVP usage baseline"
    env_vars:
      - name: PUBLIC_GA_MEASUREMENT_ID
        source: "Google Analytics Admin -> Data Streams -> Web stream -> Measurement ID"
      - name: PUBLIC_SITE_ENV
        source: "GitHub repository deployment variables; set to 'production' on production deployment"
    dashboard_config:
      - task: "Create/select GA4 web data stream for production domain"
        location: "Google Analytics Admin -> Data Streams"
      - task: "Store production measurement ID in GitHub deployment variable/secrets"
        location: "GitHub repository Settings -> Secrets and variables -> Actions"
must_haves:
  truths:
    - "When a visitor lands on any required public route, the page includes GA bootstrap ready to emit a pageview."
    - "Tracked page path uses canonical pathname only (no query/hash)."
    - "Production deployment cannot build without required GA measurement configuration."
  artifacts:
    - path: "site/src/lib/analytics.ts"
      provides: "Analytics env contract and production telemetry gate"
    - path: "site/src/layouts/BaseLayout.astro"
      provides: "Global GA script bootstrap and pageview config for all public pages"
  key_links:
    - from: "site/src/lib/analytics.ts"
      to: "site/src/layouts/BaseLayout.astro"
      via: "imported helpers/constants for telemetry gating and measurement ID"
      pattern: "from '../lib/analytics'"
    - from: "site/src/layouts/BaseLayout.astro"
      to: "https://www.googletagmanager.com/gtag/js"
      via: "async script include"
      pattern: "googletagmanager.com/gtag/js"
    - from: "site/src/layouts/BaseLayout.astro"
      to: "gtag config payload"
      via: "inline script config call with privacy flags and canonical path"
      pattern: "gtag\\('config'"
---

<objective>
Implement the production-safe GA telemetry bootstrap in the shared layout so all required public routes emit baseline pageview events.

Purpose: Deliver ANLT-01 instrumentation foundation while honoring locked decisions D-01..D-12 on route scope, canonical paths, privacy defaults, and deployment gating.
Output: Shared analytics env contract + global BaseLayout GA wiring that is production-only and build-safe.
</objective>

<execution_context>
@/home/mun/development/scipop/.opencode/get-shit-done/workflows/execute-plan.md
@/home/mun/development/scipop/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-analytics-baseline/03-CONTEXT.md
@.planning/phases/03-analytics-baseline/03-RESEARCH.md

<interfaces>
From `site/src/layouts/BaseLayout.astro`:

```ts
interface Props {
  title: string;
}
```

From `site/src/lib/paths.ts`:

```ts
export function withBase(path: string): string;
```

New contract to create in `site/src/lib/analytics.ts`:

```ts
export const SITE_ENV_PRODUCTION = 'production';
export function getSiteEnv(): string;
export function getPublicMeasurementId(): string;
export function isProductionTelemetryEnabled(): boolean;
export function assertProductionTelemetryConfig(): void;
```

Behavior contract:
- `isProductionTelemetryEnabled()` returns true only when `PUBLIC_SITE_ENV === 'production'` (per D-11).
- `assertProductionTelemetryConfig()` throws when production telemetry is enabled and measurement ID is empty (per D-10).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create analytics environment contract and production guard helpers</name>
  <read_first>site/src/lib/paths.ts, .planning/phases/03-analytics-baseline/03-CONTEXT.md, .planning/phases/03-analytics-baseline/03-RESEARCH.md</read_first>
  <files>site/src/lib/analytics.ts</files>
  <action>Create `site/src/lib/analytics.ts` with concrete helper exports: `SITE_ENV_PRODUCTION = 'production'`; `getSiteEnv()` returning `String(import.meta.env.PUBLIC_SITE_ENV ?? '').trim().toLowerCase()`; `getPublicMeasurementId()` returning trimmed `import.meta.env.PUBLIC_GA_MEASUREMENT_ID`; `isProductionTelemetryEnabled()` checking strict equality to `SITE_ENV_PRODUCTION` (per D-11); `assertProductionTelemetryConfig()` that throws `Error('PUBLIC_GA_MEASUREMENT_ID is required when PUBLIC_SITE_ENV=production')` when production telemetry is enabled and ID is empty (per D-09, D-10). Keep file dependency-free and reusable by layout + verifier logic.</action>
  <acceptance_criteria>`site/src/lib/analytics.ts` exports exactly the five contract members from plan context; file contains explicit string `PUBLIC_GA_MEASUREMENT_ID is required when PUBLIC_SITE_ENV=production`; production check is based on `PUBLIC_SITE_ENV === 'production'` only.</acceptance_criteria>
  <verify>
    <automated>npm --prefix site run build</automated>
  </verify>
  <done>Analytics env behavior is centralized and production build-guard logic exists in one importable module.</done>
</task>

<task type="auto">
  <name>Task 2: Wire GA async snippet and privacy-safe pageview config in BaseLayout</name>
  <read_first>site/src/layouts/BaseLayout.astro, site/src/lib/analytics.ts, .planning/phases/03-analytics-baseline/03-CONTEXT.md</read_first>
  <files>site/src/layouts/BaseLayout.astro</files>
  <action>Update BaseLayout frontmatter to import `assertProductionTelemetryConfig`, `getPublicMeasurementId`, and `isProductionTelemetryEnabled` from `../lib/analytics`. Compute `const telemetryEnabled = isProductionTelemetryEnabled();` and `const measurementId = getPublicMeasurementId();` then call `assertProductionTelemetryConfig()` before render (D-10, D-11). In `<head>`, conditionally render GA loader only when `telemetryEnabled` is true using async src `https://www.googletagmanager.com/gtag/js?id=${measurementId}` (D-05, D-06). Add inline bootstrap script (production-only block) that initializes `window.dataLayer`, defines `gtag`, calls `gtag('js', new Date())`, then `gtag('config', measurementId, { anonymize_ip: true, allow_google_signals: false, allow_ad_personalization_signals: false, page_path: window.location.pathname })` (D-02, D-03, D-07, D-08). Do not add history-change listeners or extra events (D-03, D-04).</action>
  <acceptance_criteria>`BaseLayout.astro` contains conditional GA script rendering gated by `telemetryEnabled`; async loader URL includes `googletagmanager.com/gtag/js?id=`; config call includes `anonymize_ip: true`, `allow_google_signals: false`, `allow_ad_personalization_signals: false`, and `page_path: window.location.pathname`; file does not contain `popstate` or `hashchange` listeners.</acceptance_criteria>
  <verify>
    <automated>npm --prefix site run build && npm --prefix site run verify:phase1</automated>
  </verify>
  <done>All BaseLayout-backed routes receive production-only GA pageview initialization with locked privacy and canonical path settings.</done>
</task>

</tasks>

<verification>
Run `npm --prefix site run build && npm --prefix site run verify:phase1`.
</verification>

<success_criteria>
- Production telemetry bootstrap exists in shared layout and is inherited by `/`, `/featured`, `/methodology`, and `/syntheses/{slug}` routes.
- Missing production measurement ID fails build before deployment.
- GA config uses canonical pathname and page-load-only initialization.
</success_criteria>

<output>
After completion, create `.planning/phases/03-analytics-baseline/03-analytics-baseline-01-SUMMARY.md`
</output>
