---
phase: 01-core-publication-surface
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - site/src/components/ClusterCard.astro
  - site/src/pages/index.astro
  - site/src/pages/syntheses/[slug].astro
autonomous: true
requirements:
  - PUBL-01
  - PUBL-02
must_haves:
  truths:
    - "Users can browse a homepage listing all approved research clusters."
    - "Users can open each approved synthesis on its own stable URL."
  artifacts:
    - path: "site/src/pages/index.astro"
      provides: "Homepage cluster listing generated from content collection"
    - path: "site/src/pages/syntheses/[slug].astro"
      provides: "Static detail route generation for approved syntheses"
  key_links:
    - from: "site/src/pages/index.astro"
      to: "site/src/pages/syntheses/[slug].astro"
      via: "anchor href using /syntheses/{slug}"
      pattern: "/syntheses/"
---

<objective>
Implement Phase 1 browse-and-open behavior for synthesis clusters.

Purpose: Deliver the core user flow where visitors discover cluster syntheses on the homepage and open each one via stable links.
Output: Homepage listing UI and dynamic synthesis detail page route.
</objective>

<execution_context>
@/home/mun/development/scipop/.opencode/get-shit-done/workflows/execute-plan.md
@/home/mun/development/scipop/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-publication-surface/01-01-PLAN.md

<interfaces>
From `site/src/content.config.ts` (created in 01-01):

```ts
// syntheses fields used here
data.title: string
data.slug: string
data.summary: string
data.cluster_id: string
data.order: number
data.status: 'approved' | 'draft'
```

Use only entries where `status === 'approved'`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build homepage cluster listing from approved syntheses</name>
  <read_first>site/src/content.config.ts, site/src/layouts/BaseLayout.astro</read_first>
  <files>site/src/components/ClusterCard.astro, site/src/pages/index.astro</files>
  <action>Implement `site/src/pages/index.astro` to load the `syntheses` collection, filter to `status: approved`, sort by `order`, and render one card per entry using `ClusterCard.astro`. Each card must include title, summary, and a link to `/syntheses/{slug}`. This task directly implements PUBL-01 and links toward PUBL-02.</action>
  <acceptance_criteria>index.astro queries syntheses collection, filters approved entries, sorts by `order`; rendered output includes links with `/syntheses/` prefix for every approved entry.</acceptance_criteria>
  <verify>
    <automated>npm --prefix site run build && npm --prefix site run verify:phase1 -- --route=/</automated>
  </verify>
  <done>Homepage is generated from real content entries and exposes linkable synthesis destinations.</done>
</task>

<task type="auto">
  <name>Task 2: Generate stable detail routes for each approved synthesis</name>
  <read_first>site/src/content.config.ts, site/src/pages/index.astro</read_first>
  <files>site/src/pages/syntheses/[slug].astro</files>
  <action>Create dynamic route `site/src/pages/syntheses/[slug].astro` with `getStaticPaths()` driven by approved `syntheses` entries. Render title, reviewed date, cluster id, and markdown body content. Ensure each path is generated from frontmatter slug and does not rely on hardcoded route lists. This implements PUBL-02.</action>
  <acceptance_criteria>[slug].astro defines getStaticPaths; static paths are produced from approved synthesis slugs; output page renders body content and metadata fields from collection data.</acceptance_criteria>
  <verify>
    <automated>npm --prefix site run build && npm --prefix site run verify:phase1 -- --route-prefix=/syntheses/</automated>
  </verify>
  <done>Every approved synthesis has a deterministic, shareable static detail URL.</done>
</task>

</tasks>

<verification>
Run `npm --prefix site run build && npm --prefix site run verify:phase1`.
</verification>

<success_criteria>
- Homepage lists approved syntheses from content, not hardcoded HTML.
- All approved synthesis entries generate detail routes under `/syntheses/{slug}`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-publication-surface/01-core-publication-surface-02-SUMMARY.md`
</output>
