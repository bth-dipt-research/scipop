---
phase: 01-core-publication-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/package.json
  - site/astro.config.mjs
  - site/tsconfig.json
  - site/src/layouts/BaseLayout.astro
  - site/src/content.config.ts
  - content/syntheses/*.md
  - content/featured/research-group.md
  - content/pages/methodology.md
  - site/scripts/verify-phase1.mjs
autonomous: true
requirements:
  - PUBL-01
  - PUBL-02
  - PUBL-03
  - PUBL-04
must_haves:
  truths:
    - "The publication app can build static pages from markdown content without runtime services."
    - "Required content types (cluster syntheses, featured synthesis, methodology) exist with validated metadata."
    - "A repeatable automated check can confirm required Phase 1 routes exist after build."
  artifacts:
    - path: "site/src/content.config.ts"
      provides: "Collection schemas for syntheses, featured, and pages"
    - path: "content/syntheses/"
      provides: "Approved synthesis markdown sources with stable slugs"
    - path: "site/scripts/verify-phase1.mjs"
      provides: "Route existence smoke verification command"
  key_links:
    - from: "site/src/content.config.ts"
      to: "content/*"
      via: "Astro glob loaders"
      pattern: "glob\(\{ base: '\\.\\./\\.\\./content"
    - from: "site/package.json"
      to: "site/scripts/verify-phase1.mjs"
      via: "npm script verify:phase1"
      pattern: "verify:phase1"
---

<objective>
Create the contract-first publication foundation for Phase 1.

Purpose: Ensure all required content destinations can be generated from validated markdown sources with stable URL contracts.
Output: Astro site scaffold, content collection schemas, seed content files, and a route smoke-check script.
</objective>

<execution_context>
@/home/mun/development/scipop/.opencode/get-shit-done/workflows/execute-plan.md
@/home/mun/development/scipop/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-publication-surface/01-RESEARCH.md
@.planning/phases/01-core-publication-surface/01-VALIDATION.md

<interfaces>
Create these contracts exactly in `site/src/content.config.ts`:

```ts
// syntheses collection schema
title: string;
slug: string;
cluster_id: string;
summary: string;
reviewed_at: Date;
status: 'approved' | 'draft';
order: number;

// featured collection schema
title: string;
slug: string;
summary: string;
reviewed_at: Date;

// pages collection schema (methodology)
title: string;
slug: string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Astro static publication app</name>
  <read_first>README.md, .planning/research/STACK.md</read_first>
  <files>site/package.json, site/astro.config.mjs, site/tsconfig.json, site/src/layouts/BaseLayout.astro</files>
  <action>Initialize a Node 22-compatible Astro project in `site/` with static output only (no SSR), add scripts `dev`, `build`, `preview`, and set `site` + `base` config for GitHub Pages project hosting. In `BaseLayout.astro`, add primary navigation links to `/`, `/featured`, and `/methodology` so discoverability is available from all pages. Do not add analytics, search, or auth (out of scope for Phase 1).</action>
  <acceptance_criteria>site/package.json contains scripts dev/build/preview; site/astro.config.mjs contains `site:` and `base:`; BaseLayout.astro contains href="/", href="/featured", href="/methodology".</acceptance_criteria>
  <verify>
    <automated>npm --prefix site run build</automated>
  </verify>
  <done>Astro app builds successfully and shared layout exposes navigation to all required top-level destinations.</done>
</task>

<task type="auto">
  <name>Task 2: Define markdown contracts and seed publication content</name>
  <read_first>site/src/content.config.ts, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</read_first>
  <files>site/src/content.config.ts, content/syntheses/cluster-*.md, content/featured/research-group.md, content/pages/methodology.md</files>
  <action>Create `site/src/content.config.ts` with three collections (`syntheses`, `featured`, `pages`) and strict required fields matching the interface block above. Add at least two approved synthesis markdown files under `content/syntheses/` with unique slugs and ordered frontmatter; add one featured markdown file at `content/featured/research-group.md`; add methodology markdown at `content/pages/methodology.md` with slug `methodology`. This implements PUBL-01/02/03/04 contract prerequisites.</action>
  <acceptance_criteria>content.config.ts exports all three collections; each markdown file contains required frontmatter keys; at least two syntheses have `status: approved` and unique `slug` values.</acceptance_criteria>
  <verify>
    <automated>npm --prefix site run build</automated>
  </verify>
  <done>Build fails on invalid content and passes when all required frontmatter fields are present.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated route smoke verification for Phase 1 destinations</name>
  <read_first>site/package.json, .planning/phases/01-core-publication-surface/01-VALIDATION.md</read_first>
  <files>site/scripts/verify-phase1.mjs, site/package.json</files>
  <action>Create `site/scripts/verify-phase1.mjs` to verify `site/dist` contains generated HTML for `/`, `/featured`, `/methodology`, and every approved synthesis slug emitted under `/syntheses/`. Add npm script `verify:phase1` that runs this checker and exits non-zero if any required route file is missing. Keep checks deterministic and file-system based.</action>
  <acceptance_criteria>package.json contains `verify:phase1`; script checks home, featured, methodology, and synthesis detail routes; command exits 0 on complete build output and non-zero on missing route.</acceptance_criteria>
  <verify>
    <automated>npm --prefix site run build && npm --prefix site run verify:phase1</automated>
  </verify>
  <done>Phase-level automated proof exists for all required route artifacts before deployment.</done>
</task>

</tasks>

<verification>
Run `npm --prefix site run build && npm --prefix site run verify:phase1` after all tasks.
</verification>

<success_criteria>
- Astro site scaffold exists and builds successfully.
- Content contracts enforce required fields for synthesis, featured, and methodology content.
- Automated route smoke checks validate required Phase 1 paths.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-publication-surface/01-core-publication-surface-01-SUMMARY.md`
</output>
