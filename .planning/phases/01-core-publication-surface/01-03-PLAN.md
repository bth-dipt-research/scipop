---
phase: 01-core-publication-surface
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - site/src/pages/featured/index.astro
  - site/src/pages/methodology.astro
  - site/scripts/verify-phase1.mjs
autonomous: true
requirements:
  - PUBL-03
  - PUBL-04
must_haves:
  truths:
    - "Users can open a dedicated featured synthesis page for the whole research group."
    - "Users can open a methodology page describing synthesis and expert review process."
  artifacts:
    - path: "site/src/pages/featured/index.astro"
      provides: "Featured synthesis destination at /featured"
    - path: "site/src/pages/methodology.astro"
      provides: "Methodology destination at /methodology"
  key_links:
    - from: "site/src/layouts/BaseLayout.astro"
      to: "site/src/pages/featured/index.astro"
      via: "navigation link /featured"
      pattern: "href=\"/featured\""
    - from: "site/src/layouts/BaseLayout.astro"
      to: "site/src/pages/methodology.astro"
      via: "navigation link /methodology"
      pattern: "href=\"/methodology\""
---

<objective>
Deliver the two remaining Phase 1 public destinations: featured synthesis and methodology.

Purpose: Complete Phase 1 publication surface requirements for top-level synthesis and process transparency pages.
Output: Static featured and methodology routes with content-driven rendering and automated route verification updates.
</objective>

<execution_context>
@/home/mun/development/scipop/.opencode/get-shit-done/workflows/execute-plan.md
@/home/mun/development/scipop/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-publication-surface/01-01-PLAN.md

<interfaces>
From `site/src/content.config.ts`:

```ts
// featured
data.title: string
data.slug: string
data.summary: string
data.reviewed_at: Date

// pages (methodology)
data.title: string
data.slug: string // expected value: "methodology"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement featured and methodology static pages from markdown collections</name>
  <read_first>site/src/content.config.ts, content/featured/research-group.md, content/pages/methodology.md</read_first>
  <files>site/src/pages/featured/index.astro, site/src/pages/methodology.astro</files>
  <action>Create `site/src/pages/featured/index.astro` to load the featured collection entry from `content/featured/research-group.md` and render title, summary, reviewed date, and body content. Create `site/src/pages/methodology.astro` to load the methodology page entry from `content/pages/methodology.md` and render process/review explanation content. Avoid hardcoded placeholder prose; render from markdown sources so updates stay editorial-first. This implements PUBL-03 and PUBL-04.</action>
  <acceptance_criteria>featured/index.astro reads featured collection data and renders markdown body; methodology.astro reads pages collection entry with slug `methodology`; both routes compile during static build.</acceptance_criteria>
  <verify>
    <automated>npm --prefix site run build && npm --prefix site run verify:phase1 -- --route=/featured --route=/methodology</automated>
  </verify>
  <done>Featured and methodology destinations are generated from content contracts and reachable at stable URLs.</done>
</task>

<task type="auto">
  <name>Task 2: Extend route smoke checker to enforce featured/methodology guarantees</name>
  <read_first>site/scripts/verify-phase1.mjs, site/package.json</read_first>
  <files>site/scripts/verify-phase1.mjs</files>
  <action>Update `verify-phase1.mjs` to explicitly fail when `/featured/index.html` or `/methodology/index.html` are missing in `site/dist`, in addition to existing home and synthesis checks. Keep CLI flags (`--route` and `--route-prefix`) supported so verification commands in other plans remain valid.</action>
  <acceptance_criteria>verify script checks `/featured` and `/methodology` by default; command still accepts route filters; exit code is non-zero when either required file is missing.</acceptance_criteria>
  <verify>
    <automated>npm --prefix site run build && npm --prefix site run verify:phase1</automated>
  </verify>
  <done>Automated verification enforces complete Phase 1 destination coverage, including featured and methodology pages.</done>
</task>

</tasks>

<verification>
Run `npm --prefix site run build && npm --prefix site run verify:phase1`.
</verification>

<success_criteria>
- `/featured` route is generated from featured markdown content.
- `/methodology` route is generated from methodology markdown content.
- Route smoke checks fail fast when either required destination is missing.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-publication-surface/01-core-publication-surface-03-SUMMARY.md`
</output>
