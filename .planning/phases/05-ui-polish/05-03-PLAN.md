---
phase: 05-ui-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - site/src/content.config.ts
  - content/syntheses/cluster-ai-and-software-engineering.md
  - content/syntheses/cluster-engineering-practices.md
  - content/syntheses/cluster-quality.md
  - content/syntheses/cluster-requirements-engineering.md
  - content/syntheses/cluster-research-methodologies.md
  - site/src/pages/syntheses/[slug].astro
  - site/scripts/verify-phase5.mjs
  - site/package.json
autonomous: true
requirements:
  - UIP-01
  - UIP-02
  - UIP-03
must_haves:
  truths:
    - "Every synthesis page includes an editor profile block near the top with required fields and standard contact sentence."
    - "Editor profile block wraps near article start on desktop and stacks above content on mobile."
    - "Phase 5 verifier enforces IA and editor-profile requirements to prevent regressions."
  artifacts:
    - path: "site/src/content.config.ts"
      provides: "Required editor metadata contract for synthesis content"
    - path: "content/syntheses/*.md"
      provides: "Concrete editor values for all approved synthesis entries"
    - path: "site/src/pages/syntheses/[slug].astro"
      provides: "Top-of-article editor profile rendering and contact sentence"
    - path: "site/scripts/verify-phase5.mjs"
      provides: "Automated Phase 5 checks for IA, long-form markers, and editor block coverage"
    - path: "site/package.json"
      provides: "`verify:phase5` script wiring"
  key_links:
    - from: "site/src/content.config.ts"
      to: "content/syntheses/*.md"
      via: "required schema fields validate editor metadata presence"
      pattern: "editor_"
    - from: "site/src/pages/syntheses/[slug].astro"
      to: "site/src/content.config.ts"
      via: "template reads required editor fields from typed synthesis entry"
      pattern: "synthesis.data.editor_"
    - from: "site/scripts/verify-phase5.mjs"
      to: "site/src/pages/syntheses/[slug].astro"
      via: "artifact assertions use stable data attributes/selectors for editor profile and IA checks"
      pattern: "data-phase5-editor-profile"
---

<objective>
Deliver synthesis editor profile requirements and Phase 5 automated verification.

Purpose: Ensure each synthesis page carries accountable editor/contact metadata and lock in phase outcomes with a dedicated verifier.
Output: Schema + content updates, synthesis template editor block, and `verify:phase5` automation.
</objective>

<execution_context>
@/home/mun/development/scipop/.opencode/get-shit-done/workflows/execute-plan.md
@/home/mun/development/scipop/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-ui-polish/05-CONTEXT.md
@.planning/phases/05-ui-polish/05-RESEARCH.md
@site/src/content.config.ts
@site/src/pages/syntheses/[slug].astro

<interfaces>
Synthesis schema additions in `site/src/content.config.ts`:

```ts
editor_name: string;
editor_email: string;
editor_photo: string;
editor_contact_sentence: string;
```

Required contact sentence contract:

```text
Questions regarding the research presented on this page? Please contact [Title] [Firstname] [Lastname]. [email]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add required editor metadata schema and populate all synthesis content entries</name>
  <read_first>site/src/content.config.ts, content/syntheses/*.md, .planning/phases/05-ui-polish/05-CONTEXT.md</read_first>
  <files>site/src/content.config.ts, content/syntheses/*.md</files>
  <action>Extend the `syntheses` collection schema with required editor fields for photo, name, email, and standard contact sentence (D-07, D-08, D-09). Update every approved synthesis markdown frontmatter file to provide values for these fields. Keep naming consistent in snake_case. Ensure contact sentence matches required wording with per-entry identity/email substitution.</action>
  <acceptance_criteria>`content.config.ts` marks editor fields as required for syntheses; each approved file under `content/syntheses` includes all required editor fields; build fails if any field is missing.</acceptance_criteria>
  <verify>
    <automated>npm --prefix site run build</automated>
  </verify>
  <done>Editor metadata is fully captured and validated at build time for all synthesis entries.</done>
</task>

<task type="auto">
  <name>Task 2: Render synthesis editor profile block and add phase verifier automation</name>
  <read_first>site/src/pages/syntheses/[slug].astro, site/scripts/verify-phase4.mjs, site/package.json</read_first>
  <files>site/src/pages/syntheses/[slug].astro, site/scripts/verify-phase5.mjs, site/package.json</files>
  <action>Inject an editor profile block near the top of synthesis pages (before main narrative body) containing editor photo, name, email, and standard contact sentence (D-07, D-08, D-09). Style layout so desktop presents top-left/wrapped behavior and mobile stacks above article content (D-10). Add long-form class/hook to ensure justified text applies to synthesis body while preserving readability defaults, and verify methodology/privacy long-form surfaces also expose justified-text hooks (D-05). Create `site/scripts/verify-phase5.mjs` to validate: `/overview` exists and `/featured` is absent; nav includes `/overview`; homepage has map and mobile fallback markers; synthesis artifacts include editor profile/contact sentence markers; long-form markers exist for synthesis, methodology, and privacy pages. Use stable selectors/data attributes for assertions instead of brittle exact-copy matching where possible. Wire `verify:phase5` in `site/package.json`.</action>
  <acceptance_criteria>`[slug].astro` renders editor block with required fields and standard sentence near page top; verifier script exists and exits non-zero on missing IA/editor/long-form markers; verifier checks use stable selectors or data attributes for editor/map/nav assertions; `site/package.json` contains `verify:phase5` script.</acceptance_criteria>
  <verify>
    <automated>npm --prefix site run build && npm --prefix site run verify:phase5</automated>
  </verify>
  <done>Editor profile UX and phase regression checks are enforceable in automated verification.</done>
</task>

</tasks>

<verification>
Run `npm --prefix site run build && npm --prefix site run verify:phase1 && npm --prefix site run verify:phase2 && npm --prefix site run verify:phase3 && npm --prefix site run verify:phase4 && npm --prefix site run verify:phase5`.
</verification>

<success_criteria>
- All synthesis pages show required editor block fields and standard contact sentence near top.
- Editor profile layout matches desktop-wrap/mobile-stack behavior.
- Automated Phase 5 verifier catches IA/editor/long-form regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-polish/05-ui-polish-03-SUMMARY.md`
</output>
