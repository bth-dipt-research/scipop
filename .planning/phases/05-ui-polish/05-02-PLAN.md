---
phase: 05-ui-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - site/src/pages/index.astro
  - site/src/components/ResearchThemeMap.astro
  - site/public/images/research-themes.svg
autonomous: true
requirements:
  - UIP-03
must_haves:
  truths:
    - "Desktop/tablet homepage uses a research-theme image-map centerpiece for navigation."
    - "Mobile homepage exposes equivalent destinations through accessible fallback links/cards."
    - "Homepage still links to all approved synthesis routes after IA shift."
  artifacts:
    - path: "site/src/pages/index.astro"
      provides: "Homepage hierarchy with desktop map and mobile fallback navigation"
    - path: "site/src/components/ResearchThemeMap.astro"
      provides: "Encapsulated map interaction and hotspot accessibility semantics"
    - path: "site/public/images/research-themes.svg"
      provides: "Static visual source for desktop/tablet map presentation"
  key_links:
    - from: "site/src/pages/index.astro"
      to: "site/src/components/ResearchThemeMap.astro"
      via: "homepage imports and renders map component"
      pattern: "ResearchThemeMap"
    - from: "site/src/components/ResearchThemeMap.astro"
      to: "content/syntheses/*.md"
      via: "hotspot destinations map to approved synthesis slug links"
      pattern: "/syntheses/"
    - from: "site/src/pages/index.astro"
      to: "site/src/components/ClusterCard.astro"
      via: "mobile fallback keeps explicit card/list navigation"
      pattern: "ClusterCard"
---

<objective>
Implement homepage IA polish with desktop map-first navigation and mobile fallback links.

Purpose: Deliver the requested image-map centerpiece while preserving reliability/accessibility on smaller devices.
Output: New map component, homepage integration, and static map asset pipeline.
</objective>

<execution_context>
@/home/mun/development/scipop/.opencode/get-shit-done/workflows/execute-plan.md
@/home/mun/development/scipop/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-ui-polish/05-CONTEXT.md
@.planning/phases/05-ui-polish/05-RESEARCH.md
@data/dev/Research themes.drawio.svg

<interfaces>
From `site/src/pages/index.astro`:

```ts
const approvedSyntheses = (await getCollection('syntheses'))
  .filter((entry) => entry.data.status === 'approved')
  .sort((left, right) => left.data.order - right.data.order);
```

New component contract in `site/src/components/ResearchThemeMap.astro`:

```ts
interface Props {
  items: Array<{ title: string; slug: string; clusterId: string }>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build desktop/tablet research-theme map component with accessible hotspots</name>
  <read_first>data/dev/Research themes.drawio.svg, site/src/components/ClusterCard.astro, .planning/phases/05-ui-polish/05-CONTEXT.md</read_first>
  <files>site/src/components/ResearchThemeMap.astro, site/public/images/research-themes.svg</files>
  <action>Create `ResearchThemeMap.astro` that renders a desktop/tablet map centerpiece derived from the research-themes source asset (D-11). Implement each hotspot as a real anchor (`<a>`) with visible focus styles, descriptive labels, and deterministic links to corresponding synthesis routes. Keep implementation static-safe (no runtime fetch). Add/copy normalized map asset under `site/public/images/research-themes.svg` for direct static serving.</action>
  <acceptance_criteria>`ResearchThemeMap.astro` exists with anchor-based hotspot links; map asset exists under `site/public/images/research-themes.svg`; component exposes labels and keyboard-focusable destinations for all mapped synthesis entries.</acceptance_criteria>
  <verify>
    <automated>npm --prefix site run build</automated>
  </verify>
  <done>Desktop/tablet map interaction is available with static-safe and accessible link semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate map-first homepage hierarchy and mobile fallback links</name>
  <read_first>site/src/pages/index.astro, site/src/components/ResearchThemeMap.astro, site/src/components/ClusterCard.astro</read_first>
  <files>site/src/pages/index.astro</files>
  <action>Refactor homepage to make the map the primary navigation element on desktop/tablet (D-11) and render card/list fallback navigation on mobile (D-12). Keep existing approved synthesis query/sort behavior and ensure all approved entries remain reachable through at least one visible navigation surface at every breakpoint. Update introductory copy to match polished IA and remove featured-route references.</action>
  <acceptance_criteria>`index.astro` renders `ResearchThemeMap` for larger breakpoints and explicit fallback links/cards for mobile; no `/featured` references remain; all approved syntheses from collection are rendered as links in at least one homepage mode.</acceptance_criteria>
  <verify>
    <automated>npm --prefix site run build && npm --prefix site run verify:phase1</automated>
  </verify>
  <done>Homepage reflects new visual hierarchy while preserving navigation reliability across devices.</done>
</task>

</tasks>

<verification>
Run `npm --prefix site run build && npm --prefix site run verify:phase1 && npm --prefix site run verify:phase4`.
</verification>

<success_criteria>
- Desktop/tablet users navigate via research-theme map links.
- Mobile users get dependable list/card fallback navigation.
- Approved synthesis destinations remain fully discoverable from homepage.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-polish/05-ui-polish-02-SUMMARY.md`
</output>
