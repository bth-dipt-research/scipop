---
phase: 05-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/layouts/BaseLayout.astro
  - site/src/pages/overview.astro
  - site/src/pages/featured/index.astro
autonomous: true
requirements:
  - UIP-01
  - UIP-03
must_haves:
  truths:
    - "Long-form pages use the polished visual system while preserving readability and accessibility baselines from Phase 02."
    - "Header shows brand logo with navigation links and no longer exposes /featured."
    - "A dedicated /overview route exists for the research-group long-form page."
  artifacts:
    - path: "site/src/layouts/BaseLayout.astro"
      provides: "Global visual tokens, logo-first header, revised navigation, and long-form typographic polish hooks"
    - path: "site/src/pages/overview.astro"
      provides: "Replacement long-form overview route"
    - path: "site/src/pages/featured/index.astro"
      provides: "Removed or retired route implementation"
  key_links:
    - from: "site/src/layouts/BaseLayout.astro"
      to: "site/src/lib/consent.ts"
      via: "existing consent script integration remains intact after UI changes"
      pattern: "readConsentState"
    - from: "site/src/layouts/BaseLayout.astro"
      to: "site/src/lib/analytics.ts"
      via: "existing telemetry gate remains intact after UI changes"
      pattern: "isProductionTelemetryEnabled"
    - from: "site/src/layouts/BaseLayout.astro"
      to: "site/src/pages/overview.astro"
      via: "header navigation links to overview destination"
      pattern: "withBase('/overview')"
---

<objective>
Implement foundational UI polish and IA migration for shared layout surfaces.

Purpose: Establish the visual system, typography direction, and navigation restructuring needed by all remaining Phase 5 work while preserving privacy/analytics/accessibility behaviors.
Output: Updated BaseLayout styles/nav/header, new `/overview` route, and retired `/featured` route.
</objective>

<execution_context>
@/home/mun/development/scipop/.opencode/get-shit-done/workflows/execute-plan.md
@/home/mun/development/scipop/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-ui-polish/05-CONTEXT.md
@.planning/phases/05-ui-polish/05-RESEARCH.md
@.planning/phases/02-readability-accessibility-baseline/02-CONTEXT.md
@.planning/phases/03-analytics-baseline/03-CONTEXT.md
@.planning/phases/04-gdpr-compliance/04-CONTEXT.md

<interfaces>
From `site/src/layouts/BaseLayout.astro`:

```ts
interface Props {
  title: string;
}
```

Navigation contract after this plan:

```html
<a href="/">Home</a>
<a href="/overview">Overview</a>
<a href="/methodology">Methodology</a>
```

Must preserve consent/analytics contract:

```ts
const telemetryEnabled = isProductionTelemetryEnabled() && consentState === 'accepted';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply global visual tokens, typography direction, and branded header/nav in BaseLayout</name>
  <read_first>site/src/layouts/BaseLayout.astro, data/dev/Grafisk manual.pdf, .planning/phases/05-ui-polish/05-CONTEXT.md</read_first>
  <files>site/src/layouts/BaseLayout.astro</files>
  <action>Update BaseLayout global styles to a subtle academic visual direction using explicit CSS custom properties for palette, typography, spacing, and focus states (D-01, D-02, D-03). Use serif body text for long-form content and sans-serif for nav/UI labels. Integrate SERL logo in header (left) with primary nav on right (D-04). Update nav items to Home, Overview, Methodology and remove Featured link (D-13, D-14). Keep existing consent and analytics script behavior unchanged, including consent gate expression and privacy settings controls from Phase 04. Keep accessibility baselines from Phase 02: readable line length, keyboard focus visibility, touch target sizing, and responsive behavior (D-06). Ensure justified long-form paragraph alignment is applied for synthesis, methodology, and privacy content surfaces (D-05) without regressing readability constraints.</action>
  <acceptance_criteria>`BaseLayout.astro` defines visual token variables; body/ui font families are differentiated (serif body, sans UI); header includes logo asset and nav contains `/overview` but not `/featured`; long-form pages (synthesis, methodology, privacy) receive justified paragraph styling hooks; consent and analytics gate code paths remain present with `isProductionTelemetryEnabled() && consentState === 'accepted'`.</acceptance_criteria>
  <verify>
    <automated>npm --prefix site run build && npm --prefix site run verify:phase1 && npm --prefix site run verify:phase4</automated>
  </verify>
  <done>Global UI language and IA shell are in place without regressing prior accessibility/privacy behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Replace featured route with overview route and migrate long-form destination</name>
  <read_first>site/src/pages/featured/index.astro, content/featured/research-group.md, site/src/lib/paths.ts</read_first>
  <files>site/src/pages/overview.astro, site/src/pages/featured/index.astro</files>
  <action>Create `site/src/pages/overview.astro` as the long-form research-group destination previously represented by featured synthesis content (D-14). Reuse existing content source and structure where possible while aligning with new page title/route semantics. Retire `/featured` by removing the route file and auditing internal links so no homepage or shared navigation references remain (D-13). Ensure overview renders through `BaseLayout` and participates in long-form typography treatment.</action>
  <acceptance_criteria>`site/src/pages/overview.astro` exists and builds; `site/src/pages/featured/index.astro` is removed; homepage/nav references no longer point to `/featured`.</acceptance_criteria>
  <verify>
    <automated>npm --prefix site run build</automated>
  </verify>
  <done>IA migration is complete: overview route exists and featured route concept is removed.</done>
</task>

</tasks>

<verification>
Run `npm --prefix site run build && npm --prefix site run verify:phase1 && npm --prefix site run verify:phase2 && npm --prefix site run verify:phase3 && npm --prefix site run verify:phase4`.
</verification>

<success_criteria>
- Shared layout uses refined visual system with preserved readability/accessibility baselines.
- Header navigation reflects new IA (`/overview` present, `/featured` absent).
- Overview route is the active long-form research-group destination.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-polish/05-ui-polish-01-SUMMARY.md`
</output>
