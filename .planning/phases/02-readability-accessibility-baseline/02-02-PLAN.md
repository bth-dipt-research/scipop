---
phase: 02-readability-accessibility-baseline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/layouts/BaseLayout.astro
autonomous: true
requirements:
  - PUBL-06
must_haves:
  truths:
    - "User can read article content on mobile and desktop without cramped line length or inconsistent spacing."
    - "User can open and use primary navigation on mobile, and desktop navigation remains visible."
    - "User can keyboard-navigate menu controls and links with visible focus indicators and usable touch targets."
  artifacts:
    - path: "site/src/layouts/BaseLayout.astro"
      provides: "Responsive typography, spacing baseline, and dual-mode navigation behavior"
  key_links:
    - from: "site/src/layouts/BaseLayout.astro"
      to: "site/src/pages/index.astro"
      via: "shared layout wrapper and inherited styles"
      pattern: "<BaseLayout"
    - from: "site/src/layouts/BaseLayout.astro"
      to: "site/src/pages/syntheses/[slug].astro"
      via: "shared layout wrapper and inherited styles"
      pattern: "<BaseLayout"
    - from: "site/src/layouts/BaseLayout.astro"
      to: "site/src/pages/featured/index.astro"
      via: "shared layout wrapper and inherited styles"
      pattern: "<BaseLayout"
---

<objective>
Implement responsive readability and accessible cross-device navigation baseline on all public pages.

Purpose: Satisfy PUBL-06 and locked decisions D-10..D-17 by enforcing readable typography and robust mobile/desktop navigation behavior.
Output: Updated shared layout with responsive reading styles and keyboard/touch-safe navigation pattern.
</objective>

<execution_context>
@/home/mun/development/scipop/.opencode/get-shit-done/workflows/execute-plan.md
@/home/mun/development/scipop/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-readability-accessibility-baseline/02-CONTEXT.md
@.planning/phases/02-readability-accessibility-baseline/02-RESEARCH.md

<interfaces>
From `site/src/layouts/BaseLayout.astro`:

```ts
interface Props {
  title: string;
}
```

Layout contract to preserve:
- Keep `<slot />` as main content insertion point.
- Keep links for `/`, `/featured`, `/methodology` in primary nav.

Add mobile-menu contract:

```html
<button type="button" aria-expanded="false" aria-controls="primary-menu">Menu</button>
<nav id="primary-menu" ...>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add responsive readability baseline styles to shared layout</name>
  <read_first>site/src/layouts/BaseLayout.astro, .planning/phases/02-readability-accessibility-baseline/02-CONTEXT.md</read_first>
  <files>site/src/layouts/BaseLayout.astro</files>
  <action>Add scoped global CSS in `BaseLayout.astro` that applies to all public pages: body text size `16px` default and `18px` at desktop breakpoint (`min-width: 1024px`) per D-11; body line-height `1.65` and heading line-height around `1.2-1.3` per D-12; article content max-width set in `65ch-75ch` range (use `max-width: 70ch`) per D-10; consistent vertical rhythm via spacing scale (for example `--space-1: 0.75rem`, `--space-2: 1rem`, `--space-3: 1.5rem`, `--space-4: 2rem`) applied to headings/paragraphs/sections per D-13.</action>
  <acceptance_criteria>`BaseLayout.astro` contains explicit typography rules with 16px base + 18px desktop, body line-height 1.65, article max-width around 70ch, and repeatable spacing token usage on content elements.</acceptance_criteria>
  <verify>
    <automated>npm --prefix site run build</automated>
  </verify>
  <done>All pages inherit stable readability defaults aligned to locked typography and spacing decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement accessible mobile menu while preserving desktop navigation</name>
  <read_first>site/src/layouts/BaseLayout.astro</read_first>
  <files>site/src/layouts/BaseLayout.astro</files>
  <action>Refactor nav markup to include desktop-visible horizontal links and mobile compact menu button (D-14, D-15). Implement a small inline script that toggles menu visibility state, updates `aria-expanded`, and closes the mobile menu after any nav link click (D-17). Add keyboard/focus/touch baseline: visible `:focus-visible` outline on button + links, minimum `min-height: 44px` and sufficient tap padding for interactive items, and ensure hidden mobile menu is not focusable when closed (D-16). Keep route targets unchanged: `/`, `/featured`, `/methodology`.</action>
  <acceptance_criteria>`BaseLayout.astro` includes menu button with `aria-expanded` + `aria-controls`; desktop nav remains visible at desktop breakpoint; mobile menu closes on link click; styles include visible focus state and `min-height: 44px` for nav controls.</acceptance_criteria>
  <verify>
    <automated>npm --prefix site run build && npm --prefix site run verify:phase1</automated>
  </verify>
  <done>Primary navigation is usable on both device classes without interaction blockers and with accessibility baseline safeguards.</done>
</task>

</tasks>

<verification>
Run `npm --prefix site run build && npm --prefix site run verify:phase1`.
</verification>

<success_criteria>
- Readability baseline applies consistently across public pages and viewport sizes.
- Mobile navigation is compact, keyboard-operable, and auto-closes after link selection.
- Desktop keeps visible horizontal navigation without regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/02-readability-accessibility-baseline/02-readability-accessibility-baseline-02-SUMMARY.md`
</output>
