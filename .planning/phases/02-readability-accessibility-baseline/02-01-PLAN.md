---
phase: 02-readability-accessibility-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/content.config.ts
  - site/src/lib/disclaimer.ts
  - site/src/pages/syntheses/[slug].astro
  - site/src/pages/featured/index.astro
  - site/scripts/verify-phase2.mjs
  - site/package.json
autonomous: true
requirements:
  - PUBL-05
must_haves:
  truths:
    - "User sees an `Important context` disclaimer section at the end of each synthesis detail page."
    - "User sees an `Important context` disclaimer section at the end of the featured synthesis page."
    - "Build fails when required narrative pages do not provide disclaimer content."
  artifacts:
    - path: "site/src/lib/disclaimer.ts"
      provides: "Shared default disclaimer text and resolver with optional per-page override"
    - path: "site/src/pages/syntheses/[slug].astro"
      provides: "Final disclaimer section render for synthesis pages"
    - path: "site/src/pages/featured/index.astro"
      provides: "Final disclaimer section render for featured page"
    - path: "site/scripts/verify-phase2.mjs"
      provides: "Automated disclaimer coverage verification"
  key_links:
    - from: "site/src/content.config.ts"
      to: "site/src/lib/disclaimer.ts"
      via: "typed disclaimer fields for resolver input"
      pattern: "disclaimer_override"
    - from: "site/src/lib/disclaimer.ts"
      to: "site/src/pages/syntheses/[slug].astro"
      via: "resolver call for per-entry disclaimer content"
      pattern: "resolveDisclaimer"
    - from: "site/src/lib/disclaimer.ts"
      to: "site/src/pages/featured/index.astro"
      via: "resolver call for featured disclaimer content"
      pattern: "resolveDisclaimer"
---

<objective>
Implement required disclaimer coverage with build-time safety checks for synthesis narrative pages.

Purpose: Satisfy PUBL-05 and locked decisions D-01..D-09 by guaranteeing a final disclaimer section with required wording and deterministic failure when missing.
Output: Typed disclaimer contract, shared resolver utility, disclaimer rendering in required pages, and automated phase verifier.
</objective>

<execution_context>
@/home/mun/development/scipop/.opencode/get-shit-done/workflows/execute-plan.md
@/home/mun/development/scipop/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-readability-accessibility-baseline/02-CONTEXT.md
@.planning/phases/02-readability-accessibility-baseline/02-RESEARCH.md

<interfaces>
From `site/src/content.config.ts`:

```ts
// existing collection fields used by this plan
syntheses.data.status: 'approved' | 'draft'
syntheses.data.slug: string
featured.data.slug: string
```

Add these concrete fields (per D-03, D-05):

```ts
disclaimer_override?: string // optional per-page copy override
```

Create resolver contract in `site/src/lib/disclaimer.ts`:

```ts
export const DISCLAIMER_HEADING = 'Important context';
export const DEFAULT_DISCLAIMER_TEXT = 'This text was generated by AI and edited by humans. It is based on SERL\'s research publications between January 2020 and September 2025. For technical questions, please contact michael.unterkalmsteiner@bth.se.';
export function resolveDisclaimer(override?: string): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define disclaimer content contract and shared resolver</name>
  <read_first>site/src/content.config.ts, .planning/phases/02-readability-accessibility-baseline/02-CONTEXT.md</read_first>
  <files>site/src/content.config.ts, site/src/lib/disclaimer.ts</files>
  <action>Update `syntheses` and `featured` collection schemas to include optional `disclaimer_override: z.string().optional()` (per D-03). Create `site/src/lib/disclaimer.ts` exporting `DISCLAIMER_HEADING = "Important context"` (per D-08), exact `DEFAULT_DISCLAIMER_TEXT` from D-09, and `resolveDisclaimer(override?: string)` that trims override and returns override when non-empty else default. Do not add disclaimer fields to `pages` collection (per D-02).</action>
  <acceptance_criteria>`content.config.ts` contains `disclaimer_override` in `syntheses` and `featured` schemas only; `site/src/lib/disclaimer.ts` exports `DISCLAIMER_HEADING`, `DEFAULT_DISCLAIMER_TEXT`, and `resolveDisclaimer` with fallback-to-default behavior.</acceptance_criteria>
  <verify>
    <automated>npm --prefix site run build</automated>
  </verify>
  <done>Disclaimer defaults and override contract exist and are type-checked by Astro content schema.</done>
</task>

<task type="auto">
  <name>Task 2: Render final disclaimer sections and add phase disclaimer verifier</name>
  <read_first>site/src/pages/syntheses/[slug].astro, site/src/pages/featured/index.astro, site/scripts/verify-phase1.mjs, site/src/lib/disclaimer.ts</read_first>
  <files>site/src/pages/syntheses/[slug].astro, site/src/pages/featured/index.astro, site/scripts/verify-phase2.mjs, site/package.json</files>
  <action>In both required narrative pages (`/syntheses/{slug}` and `/featured`), import disclaimer exports and render a final `<section aria-label="Important context">` after markdown content with heading text `Important context` and disclaimer body from `resolveDisclaimer(entry.data.disclaimer_override)` (D-01, D-04, D-08). Add explicit runtime guards that throw with clear errors when resolved disclaimer text is empty (D-05). Create `site/scripts/verify-phase2.mjs` that fails if generated routes `/featured` and each approved `/syntheses/{slug}` are missing, or if corresponding built HTML files do not include `Important context`. Add npm script `verify:phase2` in `site/package.json` to run this script.</action>
  <acceptance_criteria>Both page files contain a trailing disclaimer section with heading `Important context`; page code throws on empty disclaimer; `site/scripts/verify-phase2.mjs` exists and checks required routes + heading presence; `site/package.json` includes `"verify:phase2"` script.</acceptance_criteria>
  <verify>
    <automated>npm --prefix site run build && npm --prefix site run verify:phase2</automated>
  </verify>
  <done>Required narrative pages always end with disclaimer callout content and automated checks fail fast if disclaimer coverage regresses.</done>
</task>

</tasks>

<verification>
Run `npm --prefix site run build && npm --prefix site run verify:phase1 && npm --prefix site run verify:phase2`.
</verification>

<success_criteria>
- `/syntheses/{slug}` and `/featured` include final `Important context` disclaimer sections using shared default or override text.
- `/methodology` remains unchanged for disclaimer requirements (per D-02).
- Build or verifier fails when required disclaimer content/rendering is missing.
</success_criteria>

<output>
After completion, create `.planning/phases/02-readability-accessibility-baseline/02-readability-accessibility-baseline-01-SUMMARY.md`
</output>
